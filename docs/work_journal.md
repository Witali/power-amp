# Журнал работ проекта

Этот журнал фиксирует основные этапы работы над проектом, чтобы не зависеть от памяти чата и быстро восстанавливать контекст. Подробные технические детали остаются в исходных файлах, отчетах `results/`, заметках `study/` и истории git. В журнал стоит добавлять короткую запись после заметного изменения: что сделали, где лежат артефакты, какие проверки прошли и что стоит улучшить дальше.

## Текущий фокус

- Реконструкция и симуляция аналоговых схем, прежде всего транзисторных УМЗЧ.
- Воспроизводимые отчеты: SVG/PNG схемы, SPICE netlist, CSV-данные, графики и Markdown/HTML.
- Автоматизация повторяемых действий через `scripts/project_tasks.py`, npm-обертки и тесты.
- OCR и анализ сканов журнала `Радио`: разбиение страниц на текст, изображения, схемы и диаграммы.
- Аккуратная визуальная разметка блоков страницы с номерами, confidence score и подсветкой подписей рисунков.

## Основные артефакты

- [README.md](../README.md) - обзор проекта, tested circuits и локальный workflow.
- [results/](../results/) - результаты реконструкции, симуляции и отчетов по схемам.
- [scripts/circuitlib/](../scripts/circuitlib/) - общая библиотека для запуска вариантов схем.
- [docs/schematic_drawing_rules.md](schematic_drawing_rules.md) - правила отрисовки схем.
- [docs/plot_drawing_rules.md](plot_drawing_rules.md) - правила построения графиков.
- [docs/automation.md](automation.md) - автоматизация задач проекта.
- [part_symbols/](../part_symbols/) - библиотека условных графических обозначений.
- [study/](../study/) - исследовательские материалы, OCR-заметки и контрольные страницы.
- [study/layout_detection_marked_pages/](../study/layout_detection_marked_pages/) - PNG-превью страниц с разметкой блоков.

## Хронология

### 2026-05-15 - старт рабочей области усилителей

- Создана начальная структура проекта для исследований по усилителям.
- Подготовлены первые материалы по реконструкции схем и локальной организации результатов.
- Начали отделять сгенерированные результаты от исходных скриптов, чтобы каждую схему можно было воспроизвести.
- Коммит: `c2acfb6 Initial amplifier research workspace`.

### 2026-05-15 - ngspice и HTML-отчеты

- Добавлены первые ngspice-расчеты и отчеты по схемам.
- Появился генератор простого HTML-отчета из материалов результата.
- Проверен базовый workflow: netlist -> ngspice -> данные -> графики -> отчет.
- Коммит: `02e06ea Add ngspice simulation reports and HTML generator`.

### 2026-05-16 - схема 003 RadioStorage shema-1804-6

- Восстановлена и многократно уточнена схема усилителя `shema-1804-6`.
- Пересчитывались номиналы, ток покоя, выходной конденсатор, входной сигнал и режимы симуляции.
- Схема была сведена к рабочему варианту с вольтодобавкой, а вариант без нее убран из основных результатов.
- Обновлены импульсные и синусоидальные графики, АЧХ, КНИ и зависимость мощности.
- Ключевые коммиты: `f07cfbc`, `6630e0a`, `2d4a2a6`, `6e4b9b0`, `f272a10`.

### 2026-05-16 - общий runner и отчеты из Markdown

- Логика запуска схем вынесена в reusable workflow: `scripts/run_circuit_result.py` и `scripts/circuitlib/`.
- Для вариантов схем принят формат `results/NNN_short_name/variants/name.py`.
- Добавлен генератор HTML из Markdown, чтобы GitHub README и локальный HTML могли использовать общий источник.
- Исправлены ссылки на картинки для GitHub и ограничение ширины изображений в HTML.
- Ключевые коммиты: `2e2de1b`, `a066e62`, `8ba2863`.

### 2026-05-17 - правила отрисовки схем и библиотека символов

- Уточнены правила отрисовки схем: пропорции УГО, минимизация изгибов проводников, точки соединения, отступы от компонентов и надписей.
- Добавлена библиотека `part_symbols/` с разделением по стандартам и общими символами.
- Добавлен SVG-lint workflow, чтобы проверять корректность SVG после создания символов.
- Улучшены пропорции резисторов и общая аккуратность схемных изображений.
- Ключевые коммиты: `11462c2`, `d31e887`, `c5729f9`, `24d0eb6`.

### 2026-05-17 - OCR, Hunspell и материалы Radio.ru

- Добавлены скрипты OCR для страниц `archive.radio.ru` и локальной обработки сканов.
- Введены OCR-профили для русского текста с техническими обозначениями.
- Настроены заметки по установке локальных инструментов, включая Hunspell и словари.
- Папка временных данных переименована в `.tmp`, временные файлы вынесены из git.
- Добавлены исследования по свежим статьям об усилителях.
- Ключевые коммиты: `c196209`, `2432ad5`, `9a8a6c1`.

### 2026-05-17 - детектор макета страниц

- Создан OpenCV-детектор блоков страницы: `text`, `image`, `schematic`, `diagram`, `table`, `other`.
- Добавлены контрольные PNG-превью с цветными рамками и номерами блоков.
- Улучшено распознавание схем и диаграмм на страницах с несколькими колонками, полями и вертикальными подписями.
- Текст внутри схем подавляется, чтобы компонентные подписи не превращались в отдельные OCR-блоки.
- Добавлены тесты для классификатора, разбиения страниц, подавления вложенных блоков и геометрии контуров.
- Коммит: `ff4e336 Add OCR layout detection tooling`.

### 2026-05-17 - подписи рисунков и аккуратные рамки

- Для картинок, схем и диаграмм добавлены `caption_candidates`: кандидаты на подписи вида `Рис. 1`, `Рисунок`, `Fig.`, `Figure`.
- OCR-скрипт теперь создает `figure_links.md` и `figure_links.json`, связывая найденный номер рисунка с вырезкой блока.
- В preview добавлены порядковые номера блоков, например `#007 schematic 0.89`.
- `schematic/circuit` оставлен только как внутреннее имя класса, а в человекочитаемых подписях используется `schematic`.
- Подписи рисунков подсвечиваются желтым полупрозрачным прямоугольником.
- Для схем без отдельного текстового блока подписи добавлен осторожный lower-left probe.
- Вложенные текстовые блоки внутри более крупного текста подавляются, чтобы не было лишних рамок.
- Ключевые коммиты: `527b6f8`, `f18cf91`.

### 2026-05-18 - OpenCL-режим детектора макета

- В `scripts/detect_page_layout.py` добавлен явный параметр `--accelerator cpu|opencl`.
- OpenCL используется для безопасных OpenCV-стадий: resize, grayscale, Otsu/foreground mask и Canny. Большая morphology-стадия построения `block_mask` оставлена на CPU, потому что OpenCL менял геометрию контуров на контрольной странице.
- Добавлен воспроизводимый benchmark-скрипт `scripts/benchmark_layout_detector.py`.
- На четырех локальных страницах CPU оказался быстрее OpenCL: суммарно 10.98 s против 12.10 s, median 1.32 s против 1.51 s.
- CPU оставлен режимом по умолчанию, OpenCL доступен как экспериментальный явный режим.

### 2026-05-18 - разбиение смешанных текстово-схемных страниц

- Исправлена слишком жадная склейка крупных соседних блоков в детекторе макета: теперь тонкое касание больших текстовых и графических областей не превращает всю статью в один блок.
- Добавлены правила классификации для жирных заголовков как текста и для схем на цветной подложке как `schematic`.
- Контрольная страница `b.1997-12.015` обновлена в `study/layout_detection_marked_pages/007_b-1997-12-015_marked.png`.
- Для страниц, где OpenCV строит один огромный контур почти на весь разворот, добавлен fallback: темная типографская краска разбирается на колонки, а найденные схемы и картинки предварительно вычитаются из текстовой маски.
- Контрольная страница `b.1999-10.018` обновлена в `study/layout_detection_marked_pages/008_b-1999-10-018_marked.png`.
- Для больших текстовых областей со встроенной схемой добавлен поиск длинных горизонтальных и вертикальных линий; найденная схема расширяется по темной маске и вырезается из текстового блока.
- Контрольная страница `b.2000-09.011` обновлена в `study/layout_detection_marked_pages/009_b-2000-09-011_marked.png`.

### 2026-05-18 - частотный слой проверки макета

- Добавлен модуль `scripts/layout_frequency.py`: страница разбивается на окна, по строковым и столбцовым профилям считаются спектральные признаки, затем окна объединяются в подсказки `text`, `image`, `schematic`, `table`.
- Добавлен CLI `scripts/analyze_page_frequency.py`, который пишет `frequency_layout.json` и `frequency_preview.png`.
- `scripts/detect_page_layout.py` получил режим `--frequency-hints off|validate|hints`; по умолчанию используется `validate`, чтобы не менять геометрию OpenCV, но сохранять частотные подсказки и предупреждения в `layout.json`.
- Режим `hints` оставлен экспериментальным: он добавляет только осторожные line-art подсказки как дополнительные candidate boxes.
- Добавлены unit-тесты для FFT band-energy, объединения подсказок, mismatch-проверки и CLI.

### 2026-05-18 - объединение связанных схемных блоков

- В восстановлении текста из больших контуров уточнено вычитание визуальных областей: текстовые колонки больше не считаются картинками только из-за большого размера.
- Поиск line-art теперь использует и слишком крупные контуры страницы, поэтому схемы внутри почти полноразмерного блока находятся до повторного поиска текста.
- Добавлен повторный проход по большим областям: сначала находятся схемы, затем с их учетом добираются пропущенные текстовые колонки.
- Соседние блоки `schematic`, между которыми через маленький зазор продолжаются линии, объединяются в одну схему; мелкие служебные полоски рядом с такой схемой подавляются.
- Контрольная страница `b.2000-11.012` обновлена в `study/layout_detection_marked_pages/012_b-2000-11-012_marked.png`.

### 2026-05-18 - калибровка частотного слоя

- Константы `scripts/layout_frequency.py` пересчитаны по исходным сканам 15 уже проверенных страниц и их блокам из локальной разметки.
- Добавлен `scripts/calibrate_layout_frequency.py`, который измеряет p05/median/p95 по частотным признакам и строит confusion matrix классификатора тайлов.
- Текстовый диапазон периода строк расширен до `8..44` пикселей на 1800 px analysis scale: измеренный p05..p95 для текста составил примерно `19.2..38.4` пикселя, запас оставлен около 15-20%.
- Классификация схем теперь учитывает не только периодичность, но и высокую спектральную энтропию, согласованную плотность горизонтальных/вертикальных линий и низкую насыщенность.
- Отчет сохранен в `study/layout_frequency_calibration.md`, машинно-читаемая сводка - в `study/layout_frequency_calibration.json`.

### 2026-05-18 - micro-grid и гистограммы для частотного слоя

- Частотная сетка переведена на тайлы `32x32` с шагом `32`, чтобы получать более подробную карту страницы.
- В `tile_features` добавлены яркостная, saturation- и hue-гистограммы; яркостная гистограмма используется с наибольшим весом.
- Для текста добавлен признак яркостной бимодальности: темная краска плюс светлая бумага при небольшой доле средних яркостей.
- Для схем добавлен признак баланса темного и светлого: тонкие линии дают заметно меньшую долю темных пикселей относительно светлой бумаги, чем плотный текст или фото.
- Для маленьких тайлов удлинены line-kernel при оценке горизонтальных/вертикальных линий, чтобы буквы меньше путались с проводниками схем.
- Повторная калибровка по 15 проверенным страницам дала 30085 тайлов и точность тайлового классификатора около `0.70`; micro-grid шумнее старой сетки, но полезнее как детальный hint layer.

### 2026-05-18 - кластерный слой частотной проверки

- Добавлен независимый слой `cluster_hints`: соседние тайлы объединяются region-growing, если у них одно семейство (`text`, `image`, `line_art`) и похожий вектор признаков.
- Вектор похожести включает плотность краски, яркостный dark/light balance, долю средних яркостей, энтропию гистограмм, периодичность текста, плотность линий и насыщенность.
- В `frequency_layout.json` теперь пишутся `cluster_hints` и `cluster_hints_original`; в `layout.json` основного детектора добавлены `frequency_cluster_hints` и `frequency_cluster_warnings`.
- На preview частотного анализа обычные подсказки подписываются как `F01`, а кластерные как `C01`, чтобы их можно было сравнивать визуально.
- Для защиты от шумных областей кластеры ограничены по минимальному числу тайлов, минимальной/максимальной площади и форме служебных вертикальных полос.
- Размеченные PNG-превью теперь получают верхний заголовок с методом обработки, именем страницы и краткой сводкой блоков, чтобы картинки были понятны вне контекста чата.
- В OpenCV-постобработку добавлено слияние верхних line-art полос с соседней схемой и объединение стопки осциллограмм в один блок `diagram`; проверено на странице `b.2000-02.036`.
- На странице `b.2000-02.037` добавлена эвристика для маленьких схем с подписью: компактный монохромный line-art блок `Рис. 4` теперь определяется как `schematic`, а жирный заголовочный блок не ошибочно помечается как `diagram`.
- Добавлен общий слой разрешения пересечений блоков: спорная область классифицируется отдельно, остается у наиболее подходящего блока и вырезается из контура второго блока.
- Пороги вспомогательных histogram/balance-слоев вынесены из `compare_layout_analysis_layers.py` в отдельный модуль `layout_analysis_thresholds.py`.
- Для лучшего различения схем и диаграмм добавлен модуль `layout_component_signatures.py`: он ищет признаки типовых радиодеталей, включая прямоугольники резисторов, пары пластин конденсаторов, треугольные диодные контуры и круглые транзисторные/ламповые УГО.
- Начат экспериментальный перенос алгоритмов анализа страниц в Go: `native/go-layout` содержит standalone CLI для thresholding, connected components, базовых признаков, component signatures и JSON/crop-вывода без замены текущих Python/Node.js скриптов.
- Сохранен набор из 20 наиболее полезных страниц для регрессионных проверок OpenCV-разметки: исходники, `layout.json`, `preview.png` и манифест лежат в `study/opencv_layout_regression_pages`.

### 2026-05-19 - portable Go и проверка Go-прототипа

- Установлен portable Go `1.26.3` в `local_tools/go`; дистрибутив и кэши остаются вне git благодаря `.gitignore`.
- `scripts/setup_local_tools.ps1` теперь умеет устанавливать и проверять Go, с флагом `-SkipGo` для пропуска.
- Добавлен `scripts/test_go_layout.ps1`: он выставляет `GOCACHE`/`GOMODCACHE` внутри `.tmp`, запускает `go test ./...` и при необходимости smoke-run `layoutscan` на тестовой странице.
- В `package.json` добавлены обертки `npm run go:test` и `npm run go:smoke`.
- Go-метка схем приведена к текущему соглашению проекта: `schematic` вместо `schematic/circuit`; добавлены unit-тесты для классификации схемных признаков и имени метки.
- Добавлен инвентарь скачиваемых локальных инструментов `docs/downloaded_tools.md`; новое правило проекта требует обновлять его при добавлении скачиваемых инструментов.
- Корневой `init.ps1` теперь прокидывает `-SkipGo` в `scripts/setup_local_tools.ps1`.
- По ручной разметке страницы `b.2000-01.041` доработан OpenCV-пайплайн: вложенные схемы, фото и диаграммы внутри крупных текстовых блоков теперь ищутся отдельным line-art проходом; стопки осциллограмм объединяются в `diagram` до слияния схемных блоков.
- Контрольное превью сохранено как `study/layout_detection_marked_pages/013_b-2000-01-041_marked.png`.
- Разбиение склеенных текстовых колонок усилено проверкой вертикальных белых коридоров: если через блок проходит достаточно широкий столбец почти без темных пикселей, он используется как кандидат на разделитель колонок.
- Добавлен генератор HTML-отчета `scripts/generate_opencv_layout_report.py` / `npm run layout:report`: он заново прогоняет регрессионные страницы через OpenCV и выводит пары кликабельных превью `original` / `OpenCV markup`.
- Для компактных схем уточнено слияние соседних фрагментов: пересекающиеся схемные блоки и узкие технические подписи внутри схемы объединяются, но крупные схемы и боковые полосы больше не поглощают соседний текст.
- Добавлен корневой проектный индекс `index.html`, генерируемый командой `npm run project:index`; он связывает результаты схем, отчет распознавания страниц и библиотеку УГО.
- Для страницы `b.2000-11.011` добавлено слияние журнальных line-art иллюстраций с внутренними подписями: фрагменты `text`/`other`/`diagram`, геометрически принадлежащие одному рисунку, собираются в единый блок `image`, а соседний обычный текст остается отдельным.
- Подписи блоков на preview теперь привязываются к видимой верхней кромке контура. Это убирает плавающие label-плашки у L-образных рамок, появляющихся после вычитания текстовых областей из схем.
- Для страницы `b.1997-12.016` добавлено слияние тонких рамочных strip-блоков со схемой: высокая правая часть рамки больше не определяется как отдельный `diagram`, а расширяет блок `schematic`.
- Для страницы `b.2000-11.013` убраны длинные тонкие отростки контура схемы в текстовую колонку: если два текстовых выреза стоят в одной колонке у края визуального блока, зазор между ними тоже вырезается из preview-контура.
- Введен отдельный тип блока `pcb` для печатных плат. Классификатор учитывает плотность толстых проводящих дорожек, круглые контактные площадки и прямоугольный контур платы; на странице `b.2000-01.042` верхний рисунок платы теперь отделяется от принципиальных схем.
- Основные числовые пороги и цвета layout/OCR-пайплайна вынесены в `scripts/layout_config.py`: классы блоков, цвета preview, PCB-пороги, частотные признаки, гистограммные проверки и параметры сигнатур компонентов теперь собраны в одном месте.
- По текущей разметке `b.2000-01.042` добавлено разделение широких многострочных текстовых блоков по вертикальному межколоночному просвету и подавление маленьких технических подписей, прижатых к PCB/схемам. Старый блок `#011` теперь делится на две колонки, а размерные подписи у платы больше не попадают в OCR-текст.
- Создан результат `004_radiostorage_shema_1804_6_darlington`: вариант схемы 003 с верхним плечом Дарлингтона, добавленным `VT2A`, третьим диодом смещения, SPICE-netlist, PNG/SVG-схемой, графиками и README.
- Для результата 004 график КНИ от частоты строится тремя кривыми при целевых мощностях 20, 50 и 100 мВт; уровень входного сигнала подбирается бисекцией по выходной мощности.
- Для результата 004 график КНИ от выходной мощности расширен до нескольких частот: 100 Гц, 500 Гц, 1 кГц, 5 кГц и 10 кГц.
- В OpenCV-разметку добавлен отдельный текстовый тип `heading`, чтобы крупные насыщенные заголовки вроде блока `#003` на странице `b.1997-04.018` не классифицировались как `image`.
- Описание OpenCV pipeline сохранено рядом со скриптом в `scripts/detect_page_layout_pipeline.md`.
- Для результата 004 пересчитаны стандартные E24-номиналы `R1A = 510 Ом`, `R1B = 3.3 кОм`, `R2 = 56 кОм`: рабочая точка стала около `V(out) = 5.995 В`, токи выходных транзисторов около `8.3 мА`. Схема перерисована: C4 больше не пересекает проводники, выходная шина отодвинута от корпусов VT2B/VT3, а смещение VT1 от общей точки выходного каскада показано явным проводом.
- В правила отрисовки схем добавлен минимальный зазор около четверти размера компонента между корпусом/УГО и посторонними проводниками или надписями.
- Для результата 004 заменен эмиттерный резистор VT1 на стандартный номинал `R4 = 51 Ом`; после пересчета E24-номиналы стали `R1A = 620 Ом`, `R1B = 3.3 кОм`, `R2 = 62 кОм`. Рабочая точка по ngspice: `V(out) = 6.001 В`, токи выходных транзисторов около `7.9 мА`, ток питания около `9.24 мА`.

## Что проверять после изменений

- `python -m unittest discover -s tests`
- `scripts/test_go_layout.ps1 -SkipScan` или `npm run go:test`
- `git diff --check`
- PowerShell parser для измененных `.ps1`-скриптов, если менялся PowerShell-код.
- Визуальный просмотр PNG-превью для страниц из `study/layout_detection_marked_pages/`.
- Для схемных результатов: повторный запуск соответствующего варианта через `scripts/run_circuit_result.py` или `scripts/project_tasks.py result`.

## Открытые заметки

- Страница `study/layout_detection_marked_pages/006_b-1998-12-018_marked.png` полезна как отрицательный пример: схема на ней пока захватывается как часть текстового блока.
- Для OCR статей нужно продолжать проверять разбиение на колонки перед массовым распознаванием.
- При загрузке страниц с `archive.radio.ru` использовать последовательные или малопараллельные запросы, чтобы не создавать лишнюю нагрузку на сайт.
- Confidence score на превью - это внутренний балл уверенности классификатора, а не строгая статистическая вероятность.
